@model IEnumerable<DAL.Brand>

<div class="container-fluid">

    @if (Model.Count() > 0)
    {
        <h2 class="h2 text-center fw-bolder" style="color:darkgray"> Brands </h2>
        <div class="row mt-2">
            <table class="table table-responsive table-striped">

                <thead style="color:olivedrab">
                    <tr>
                        <th>
                            @Html.Label("Brand Id")
                        </th>
                        <th>
                            @Html.Label("Brand Name")
                        </th>

                        <th>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var prd in Model)
                    {
                        <tr>
                            <td>
                                @prd.BrandId
                            </td>
                            <td>
                                @prd.BrandName
                            </td>

                            <td>
                                @Html.ActionLink("Edit", "EditBrand", "Brand", new { area = "Admin" }, new { @class = "btn btn-sm btn-success" })
                                @Html.ActionLink("Delete", "DeleteBrand", "Brand", new { area = "Admin" }, new { @class = "btn btn-sm btn-danger" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="row">
            <p class="text-capitalize fw-bolder .textColor">
                No Brand's'
            </p>
        </div>
    }
</div>