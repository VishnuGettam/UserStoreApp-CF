<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>UserStore</title>
    <link rel="stylesheet" href="~/Content/bootstrap.css" />
</head>
<body>
    <div>

        <nav class="navbar navbar-expand navbar-dark bg-success">
            <div class="navbar-brand">
                @Html.ActionLink("UserStore", "Index", "Product", new { area = "Manager" }, new { @class = "nav-link fw-bolder", style = "color:coral" })
            </div>
            <ul class="navbar-nav navbar-collapse">

                <li class="nav-item">
                    @Html.ActionLink("Products", "Index", "Product", new { area = "Manager" }, new { @class = "nav-link" })
                </li>
            </ul>


            @if (User.Identity.IsAuthenticated)
            {
                <div class="row">
                    <div class="col-1 d-flex fw-bolder ">
                        Hi,<span class="fst-italic" style="color:darkmagenta"> @User.Identity.Name </span>
                    </div>
                </div>
                <div class="row">
                    <div class="col-1">

                        @Html.ActionLink("Logout", "Logout", "Account", new { area = "" }, new { @class = "btn btn-sm btn-info m-2" })

                    </div>
                </div>

            }
        </nav>

        @RenderBody()

        <script type="text/javascript" src="~/Scripts/jquery-3.6.0.js"></script>
        <script type="text/javascript" src="~/Scripts/bootstrap.js"></script>
    </div>
</body>
</html>